name: TEST-PARENT

on:
  workflow_dispatch:

jobs:
  start-parent:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Run parent"

  call-child:
    name: child
    needs: [ start_pipeline ]
    uses: ./.github/workflows/child.yml

  finish-success:
    needs: [ call-child ]
    runs-on: ubuntu-latest
    if: ${{ success() }}
    steps:
      - run: |
          echo "Success"

  finish-failure:
    needs: [ call-child ]
    runs-on: ubuntu-latest
    if: ${{ failure() }}
    steps:
      - run: |
          echo "Failure"
